"use strict";
{

  (function () {
    const target = '.card .splide1';
    const options = {
      mediaQuery: 'min',
      perMove: 1,
      fixedWidth: '240px',
      gap: 16,
      updateOnMove: true,
      breakpoints: {
        1025: {
          fixedWidth: '360px',
          gap: 32,
        }
      },
    }
    const mySplide = new Splide(target, options);

    mySplide.mount();
  })();

  (function () {
    const target = '.card .splide2';
    const options = {
      mediaQuery: 'min',
      perMove: 1,
      fixedWidth: '240px',
      gap: 16,
      updateOnMove: true,
      breakpoints: {
        1025: {
          fixedWidth: '360px',
          gap: 32,
        }
      },
    }
    const mySplide = new Splide(target, options);

    mySplide.mount();
  })();

  (function () {
    const target = '.card .splide3';
    const options = {
      mediaQuery: 'min',
      perMove: 1,
      fixedWidth: '240px',
      gap: 16,
      updateOnMove: true,
      breakpoints: {
        1025: {
          fixedWidth: '360px',
          gap: 32,
        }
      },
    }
    const mySplide = new Splide(target, options);

    mySplide.mount();
  })();

  (function () {
    const target = '.card .splide4';
    const options = {
      mediaQuery: 'min',
      perMove: 1,
      fixedWidth: '240px',
      gap: 16,
      updateOnMove: true,
      breakpoints: {
        1025: {
          fixedWidth: '360px',
          gap: 32,
        }
      },
    }
    const mySplide = new Splide(target, options);

    mySplide.mount();
  })();

  (function () {
    const target = '.card .splide5';
    const options = {
      mediaQuery: 'min',
      perMove: 1,
      fixedWidth: '240px',
      gap: 16,
      updateOnMove: true,
      breakpoints: {
        1025: {
          fixedWidth: '360px',
          gap: 32,
        }
      },
    }
    const mySplide = new Splide(target, options);

    mySplide.mount();
  })();

  (function () {
    const target = '.card .splide6';
    const options = {
      mediaQuery: 'min',
      perMove: 1,
      fixedWidth: '240px',
      gap: 16,
      updateOnMove: true,
      breakpoints: {
        1025: {
          fixedWidth: '360px',
          gap: 32,
        }
      },
    }
    const mySplide = new Splide(target, options);

    mySplide.mount();
  })();
}

